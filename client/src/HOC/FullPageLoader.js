import React from 'react'

const FullPageLoader = (wrappedComponent) => {
   return class FullPageLoader extends React.Component {
      render() {
         return (
            
         )
      }
   }
}

export default FullPageLoader